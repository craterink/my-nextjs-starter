import "react";

const classes = {
  bodyWrapper: {
    display: "flex",
    alignItems: "center",
    justifyContent: "center",
    height: "100%",
  },
} as Record<string, React.CSSProperties>;

const Component = () => {
  return <></>;
};

export default Component;
